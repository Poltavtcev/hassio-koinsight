---
# Legacy reference only — Supervisor 2026.04+ ignores build.yaml and may inject a
# stale/wrong BUILD_FROM (e.g. maziggy). The active base image is the Dockerfile FROM line.
# Fork (default):  FROM ghcr.io/poltavtcev/bambuddy:latest
# Upstream preset: see build.upstream.yaml
